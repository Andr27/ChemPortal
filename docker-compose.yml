version: '3.9'

services:

  redis:
    image: redis:7-alpine
    container_name: portal_redis

    ports:
      - "6379:6379"

    restart: always

    command: redis-server --save 60 1 --loglevel warning